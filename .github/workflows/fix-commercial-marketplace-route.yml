name: Fix LISTIA commercial marketplace route (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired: the commercial Marketplace builder is validated by the canonical commercial deployment workflow.'
          echo 'Do not patch scripts/build-commercial-worker.py from one-shot repair workflows.'
