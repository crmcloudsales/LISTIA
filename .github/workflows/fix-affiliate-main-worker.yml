name: Retired LISTIA affiliate main Worker patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Affiliate routing is already canonical in scripts/build-commercial-worker.py."
