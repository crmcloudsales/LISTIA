name: LISTIA Brand Routing Repair v9 (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired: commercial brand routes are now canonical in scripts/build-commercial-worker.py.'
          echo 'This workflow must not mutate the builder or restore legacy cross-product resources.'
