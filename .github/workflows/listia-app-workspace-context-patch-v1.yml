name: Retired LISTIA app workspace context patch v1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Canonical active-workspace selection is already present in the core app."
