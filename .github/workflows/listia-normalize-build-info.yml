name: LISTIA Normalize Build Info (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Point to canonical release metadata workflow
        run: |
          echo 'This workflow no longer mutates BUILD_INFO.json.'
          echo 'Canonical release metadata is generated by LISTIA Release Metadata.'
          echo 'See public/BUILD_INFO.json and scripts/release-listia-metadata.py.'
