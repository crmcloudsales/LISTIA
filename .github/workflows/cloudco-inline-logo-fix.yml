name: LISTIA Legacy CloudCo Logo Fix (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. LISTIA commercial runtime must not embed or restore CloudCo brand assets.'
          echo 'Use the LISTIA-owned /investment route and listia-investment-lead endpoint.'
