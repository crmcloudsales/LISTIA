name: Retired LISTIA Brand Refresh v4

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. LISTIA branding is locked by the official v8 validation workflow."
