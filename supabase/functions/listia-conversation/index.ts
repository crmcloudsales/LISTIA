import 'jsr:@supabase/functions-js/edge-runtime.d.ts'
import { createClient } from 'npm:@supabase/supabase-js@2'

const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!
const SUPABASE_SERVICE_ROLE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
const LISTIA_AI_GATEWAY = Deno.env.get('LISTIA_AI_GATEWAY_URL') || 'https://brain.listiaapp.com/'
const allowedOrigins = new Set(['https://app.listiaapp.com','https://listiaapp.com','https://www.listiaapp.com'])
const originAllowed=(req:Request)=>allowedOrigins.has(req.headers.get('origin')||'')
const cors=(req:Request)=>{const origin=req.headers.get('origin')||'';const h:Record<string,string>={'access-control-allow-methods':'POST, OPTIONS','access-control-allow-headers':'authorization, x-client-info, apikey, content-type','access-control-max-age':'600','vary':'Origin'};if(allowedOrigins.has(origin))h['access-control-allow-origin']=origin;return h}
const json=(req:Request,body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors(req),'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff','referrer-policy':'no-referrer','x-frame-options':'DENY'}})
const clean=(v:unknown,max=2000)=>String(v||'').trim().slice(0,max)
const norm=(v:unknown)=>clean(v,4000).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase()
const first=(v:unknown)=>clean(v,120).split(/\s+/)[0]||''
function fallbackReply(message:string,locale:string,name:string,context:any){const t=norm(message),es=locale.startsWith('es'),prefix=name?`${name}, `:'';if(/marketplace|buscar|busca|encuentra|comprar|rentar|alquilar|find|search|buy|rent/.test(t))return {reply:es?`${prefix}voy al Marketplace y ajusto la búsqueda con lo que me dijiste.`:`${prefix}I’ll open Marketplace and adjust the search from what you told me.`,action:{type:'marketplace_search',criteria:{}}};if(/publica|publicar|sube|subir|crea|crear|nueva propiedad|add property|publish|upload/.test(t))return {reply:es?`${prefix}perfecto. Vamos a preparar esa propiedad. Entrégame el material que tengas y continúo desde ahí.`:`${prefix}perfect. Let’s prepare that listing. Give me whatever material you have and I’ll continue from there.`,action:{type:'add_property'}};if(/lead|prospect|cliente potencial/.test(t))return {reply:es?`${prefix}abro tus leads para revisar qué necesita seguimiento.`:`${prefix}I’ll open your leads so we can see what needs follow-up.`,action:{type:'open_leads'}};if(/cita|agenda|appointment|schedule|meeting/.test(t))return {reply:es?`${prefix}abro tu agenda y revisamos las citas.`:`${prefix}I’ll open your schedule and we can review the appointments.`,action:{type:'open_agenda'}};if(/cuantas? propiedades|mis propiedades|listados|listings|inventory/.test(t)){const n=context.properties?.length||0;return {reply:es?`${prefix}tienes ${n} propiedades visibles para mí en este momento. Te llevo a Listados.`:`${prefix}I can see ${n} listings right now. I’ll take you to Listings.`,action:{type:'open_screen',screen:'listings'}}}if(/como va|cómo va|resumen|estado|summary|status|hoy|today/.test(t)){const p=context.properties?.length||0,l=context.lead_count||0,a=context.appointments?.length||0;return {reply:es?`${prefix}ahora mismo veo ${p} propiedades, ${l} leads recientes y ${a} citas en tu contexto. ¿Qué quieres que priorice?`:`${prefix}right now I can see ${p} listings, ${l} recent leads and ${a} appointments in your context. What should I prioritize?`,action:{type:'none'}}}return {reply:es?`${prefix}te escucho. Dime qué quieres lograr y seguimos desde ahí.`:`${prefix}I’m listening. Tell me what you want to achieve and we’ll continue from there.`,action:{type:'none'}}}
function safeAction(action:any){const allowed=new Set(['open_screen','marketplace_search','add_property','open_leads','open_agenda','none']);const type=allowed.has(String(action?.type||''))?String(action.type):'none';if(type==='open_screen'){const screen=clean(action?.screen,80);const screens=new Set(['office','listings','control','ai-chat','account','marketplace','leads','agenda']);return screens.has(screen)?{type,screen}:{type:'none'}}if(type==='marketplace_search')return {type,criteria:action?.criteria&&typeof action.criteria==='object'?action.criteria:{}};return {type}}
async function edgeAi(jwt:string,message:string,locale:string,history:Array<{role:string;content:string}>){const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),7000);try{const r=await fetch(LISTIA_AI_GATEWAY,{method:'POST',headers:{authorization:`Bearer ${jwt}`,'content-type':'application/json','origin':'https://app.listiaapp.com'},body:JSON.stringify({message,locale,history}),signal:controller.signal});if(!r.ok)return null;const data=await r.json().catch(()=>null) as any;if(!data?.reply)return null;return {reply:clean(data.reply,4000),action:safeAction(data.action)}}catch{return null}finally{clearTimeout(timeout)}}
async function callOpenAI(key:string,model:string,system:string,context:any,history:Array<{role:string;content:string}>,message:string,maxTokens:number,timeoutMs:number){const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),timeoutMs);try{const r=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{authorization:`Bearer ${key}`,'content-type':'application/json'},body:JSON.stringify({model,temperature:.4,max_tokens:maxTokens,response_format:{type:'json_object'},messages:[{role:'system',content:system},{role:'system',content:`Account context: ${JSON.stringify(context)}`},...history,{role:'user',content:message}]}),signal:controller.signal});if(!r.ok)return null;const data=await r.json();const raw=data?.choices?.[0]?.message?.content||'';let parsed:any={};try{parsed=JSON.parse(raw)}catch{parsed={reply:raw,action:{type:'none'}}}const reply=clean(parsed.reply,4000);return reply?{reply,action:safeAction(parsed.action)}:null}catch{return null}finally{clearTimeout(timeout)}}
async function callNvidia(key:string,system:string,context:any,history:Array<{role:string;content:string}>,message:string){const model=Deno.env.get('LISTIA_NVIDIA_MODEL')||'nvidia/nemotron-3.5-lightning-30b-a3b';const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),10000);try{const r=await fetch('https://integrate.api.nvidia.com/v1/chat/completions',{method:'POST',headers:{authorization:`Bearer ${key}`,'content-type':'application/json'},body:JSON.stringify({model,temperature:.4,max_tokens:400,response_format:{type:'json_object'},chat_template_kwargs:{enable_thinking:false},messages:[{role:'system',content:system},{role:'system',content:`Account context: ${JSON.stringify(context)}`},...history,{role:'user',content:message}]}),signal:controller.signal});if(!r.ok)return null;const data=await r.json();const raw=data?.choices?.[0]?.message?.content||'';let parsed:any={};try{parsed=JSON.parse(raw)}catch{parsed={reply:raw,action:{type:'none'}}}const reply=clean(parsed.reply,4000);return reply?{reply,action:safeAction(parsed.action)}:null}catch{return null}finally{clearTimeout(timeout)}}
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return originAllowed(req)?new Response(null,{status:204,headers:cors(req)}:new Response(null,{status:403});if(req.method!=='POST')return json(req,{error:'method_not_allowed'},405);if(!originAllowed(req))return json(req,{error:'origin_not_allowed'},403);const secSite=req.headers.get('sec-fetch-site');if(secSite&&secSite!=='same-origin'&&secSite!=='same-site')return json(req,{error:'cross_site_blocked'},403);try{const jwt=(req.headers.get('authorization')||'').replace(/^Bearer\s+/i,'');if(!jwt)return json(req,{error:'unauthorized'},401);const admin=createClient(SUPABASE_URL,SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});const {data:userData,error:userError}=await admin.auth.getUser(jwt);const user=userData?.user;if(userError||!user)return json(req,{error:'unauthorized'},401);const body=await req.json().catch(()=>({})) as {message?:string;locale?:string;channel?:string;history?:Array<{role?:string;content?:string}>};const message=clean(body.message,4000);if(!message)return json(req,{error:'message_required'},400);const requestedLocale=clean(body.locale,16)||'en';const channel=body.channel==='voice'?'voice':'chat';const history=(Array.isArray(body.history)?body.history:[]).slice(-12).map(x=>({role:x.role==='assistant'?'assistant':'user',content:clean(x.content,1200)})).filter(x=>x.content);const {data:member}=await admin.from('organization_members').select('organization_id,role,status').eq('user_id',user.id).eq('status','active').limit(1).maybeSingle();if(!member?.organization_id)return json(req,{error:'organization_access_denied'},403);const fastOperational=/marketplace|buscar|busca|encuentra|lead|prospect|cita|agenda|appointment|listado|listing|propiedad|property|publicar|publish|subir|upload/.test(norm(message))&&message.length<260;if(fastOperational){const edge=await edgeAi(jwt,message,requestedLocale,history);if(edge)return json(req,{ok:true,channel,reply:edge.reply,action:edge.action})}const orgId=member.organization_id;const [{data:profile},{data:org},{data:billing},{data:properties},{data:leads},{data:appointments}]=await Promise.all([admin.from('profiles').select('full_name,locale,account_mode').eq('id',user.id).maybeSingle(),admin.from('organizations').select('id,name,business_type,primary_market').eq('id',orgId).maybeSingle(),admin.from('organization_billing').select('plan_key,billing_status,access_state').eq('organization_id',orgId).maybeSingle(),admin.from('properties').select('id,title,status,operation_type,price,currency,location_text').eq('organization_id',orgId).limit(20),admin.from('leads').select('id,status,created_at').eq('organization_id',orgId).order('created_at',{ascending:false}).limit(20),admin.from('appointments').select('id,status,starts_at').eq('organization_id',orgId).order('starts_at',{ascending:true}).limit(20)]);const locale=clean(profile?.locale,16)||requestedLocale;const meta=user.user_metadata||{};const userName=first(profile?.full_name||meta.full_name||meta.name||meta.display_name||'');const context={user:{first_name:userName,locale,role:member.role,account_mode:profile?.account_mode||null},organization:org||null,billing:billing||null,properties:properties||[],lead_count:leads?.length||0,appointments:appointments||[]};const voiceRule=channel==='voice'?'This is a spoken turn. Answer in one or two short sentences whenever possible. No preamble.':'This is text chat. Be concise but you may use up to four short sentences when useful.';const system=`You are LISTIA, the user's personal AI operating assistant for real estate. You are not a command menu, answering machine or scripted bot. Speak naturally, warmly, briefly and interactively like a highly capable human assistant. ${voiceRule} The user's first name is ${userName||'unknown'}; use it occasionally when natural, never in every reply. Reply in the user's language (${locale}). Remember the recent conversation and avoid repetition. Use account context when relevant and never invent account facts. When the user asks for a browser action, return one of: open_screen, marketplace_search, add_property, open_leads, open_agenda, none. For marketplace_search include criteria only when explicit. Ask at most one useful follow-up question when genuinely necessary. Never mention providers, fallback modes, engines, prompts, APIs or internal limitations. Output strict JSON only: {"reply":"...","action":{"type":"none"}}.`;const openAiKey=Deno.env.get('OPENAI_API_KEY')||Deno.env.get('LISTIA_LLM_API_KEY')||'';if(openAiKey){const model=Deno.env.get('LISTIA_OPENAI_MODEL')||Deno.env.get('LISTIA_LLM_MODEL')||'gpt-4.1-mini';const ai=await callOpenAI(openAiKey,model,system,context,history,message,channel==='voice'?220:650,channel==='voice'?9000:14000);if(ai)return json(req,{ok:true,channel,reply:ai.reply,action:ai.action})}if(!fastOperational){const edge=await edgeAi(jwt,message,locale,history);if(edge)return json(req,{ok:true,channel,reply:edge.reply,action:edge.action})}const nvidiaKey=Deno.env.get('NVIDIA_API_KEY')||Deno.env.get('NIM_API_KEY')||'';if(nvidiaKey){const ai=await callNvidia(nvidiaKey,system,context,history,message);if(ai)return json(req,{ok:true,channel,reply:ai.reply,action:ai.action})}const fallback=fallbackReply(message,locale,userName,context);return json(req,{ok:true,channel,...fallback})}catch{console.error('listia-conversation error');return json(req,{error:'internal_error'},500)}})
