import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const U=Deno.env.get('SUPABASE_URL')||'',A=Deno.env.get('SUPABASE_ANON_KEY')||Deno.env.get('SUPABASE_PUBLISHABLE_KEY')||'',S=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'';
const EXTERNAL_DOMAINS_ENABLED=Deno.env.get('LISTIA_EXTERNAL_DOMAINS_ENABLED')==='true';
const allowed=new Set(['https://app.listiaapp.com','http://localhost','http://127.0.0.1']);
const reserved=new Set(['app','www','api','admin','mail','smtp','ftp','marketplace','web','brain','ai']);
const reply=(b:unknown,s=200,o='')=>new Response(JSON.stringify(b),{status:s,headers:{'content-type':'application/json','cache-control':'no-store','x-content-type-options':'nosniff',...(o&&allowed.has(o)?{'access-control-allow-origin':o,'vary':'Origin'}:{})}});
Deno.serve(async req=>{const o=req.headers.get('origin')||'';if(req.method==='OPTIONS'){if(!allowed.has(o))return new Response(null,{status:403});return new Response(null,{status:204,headers:{'access-control-allow-origin':o,'access-control-allow-headers':'authorization,apikey,content-type','access-control-allow-methods':'POST,OPTIONS','vary':'Origin'}})}if(req.method!=='POST')return reply({error:'method_not_allowed'},405,o);if(o&&!allowed.has(o))return reply({error:'origin_not_allowed'},403,o);const auth=req.headers.get('authorization')||'';if(!auth.startsWith('Bearer ')||!U||!A||!S)return reply({error:'unauthorized'},401,o);const ur=await fetch(`${U}/auth/v1/user`,{headers:{apikey:A,authorization:auth}});if(!ur.ok)return reply({error:'unauthorized'},401,o);const user=await ur.json(),uid=String(user?.id||'');let body:any={};try{body=await req.json()}catch{return reply({error:'invalid_json'},400,o)}const oid=String(body.organization_id||''),mode=String(body.mode||'');if(!oid)return reply({error:'organization_required'},400,o);const ah={apikey:S,authorization:`Bearer ${S}`};const mr=await fetch(`${U}/rest/v1/organization_members?select=role,status&organization_id=eq.${encodeURIComponent(oid)}&user_id=eq.${encodeURIComponent(uid)}&status=eq.active&limit=1`,{headers:ah});const mm=mr.ok?(await mr.json())?.[0]:null;if(!mm||!['owner','admin'].includes(String(mm.role)))return reply({error:'forbidden'},403,o);
if(mode==='listia_subdomain'){const sub=String(body.subdomain||'').trim().toLowerCase();if(!/^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$/.test(sub)||reserved.has(sub))return reply({error:'invalid_subdomain'},400,o);const cr=await fetch(`${U}/rest/v1/organization_websites?select=organization_id&mode=eq.listia_subdomain&subdomain=ilike.${encodeURIComponent(sub)}&organization_id=neq.${encodeURIComponent(oid)}&limit=1`,{headers:ah});if(cr.ok&&(await cr.json()).length)return reply({error:'subdomain_taken'},409,o);const row={organization_id:oid,mode:'listia_subdomain',domain:null,subdomain:sub,status:'active',connect_fee_usd:null,domain_markup_percent:null,provider_cost_usd:null,final_price_usd:null,configuration:{managed_by:'LISTIA',seo_continuous:true,junk_lead_firewall:true,waf:true,turnstile:true,web_events:true,pixel_ready:true,provisioned_via:'wildcard',activated_at:new Date().toISOString()}};const wr=await fetch(`${U}/rest/v1/organization_websites?on_conflict=organization_id`,{method:'POST',headers:{...ah,'content-type':'application/json','prefer':'resolution=merge-duplicates,return=representation'},body:JSON.stringify(row)});if(!wr.ok)return reply({error:'provision_failed'},502,o);return reply({ok:true,status:'active',hostname:`${sub}.listiaapp.com`,website:(await wr.json())?.[0]||null},200,o)}
if(mode==='connect_existing'||mode==='buy_website'){if(!EXTERNAL_DOMAINS_ENABLED)return reply({error:'external_domains_temporarily_unavailable',reason:'secure_ssl_provisioning_not_enabled',listia_subdomain_available:true,charge_created:false},503,o);const domain=String(body.domain||'').trim().toLowerCase().replace(/^https?:\/\//,'').replace(/\/.*$/,'').replace(/^www\./,'');if(!/^(?=.{3,253}$)(?:[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z]{2,63}$/i.test(domain))return reply({error:'invalid_domain'},400,o);const row={organization_id:oid,mode,domain,subdomain:null,status:'selected',connect_fee_usd:mode==='connect_existing'?10:null,domain_markup_percent:mode==='buy_website'?100:null,provider_cost_usd:null,final_price_usd:null,configuration:{managed_by:'LISTIA',seo_continuous:true,junk_lead_firewall:true,waf:true,turnstile:true,web_events:true,pixel_ready:true}};const wr=await fetch(`${U}/rest/v1/organization_websites?on_conflict=organization_id`,{method:'POST',headers:{...ah,'content-type':'application/json','prefer':'resolution=merge-duplicates,return=representation'},body:JSON.stringify(row)});if(!wr.ok)return reply({error:'save_failed'},502,o);return reply({ok:true,status:'selected',hostname:domain,website:(await wr.json())?.[0]||null},200,o)}return reply({error:'unsupported_mode'},400,o)});