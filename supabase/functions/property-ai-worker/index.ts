import 'jsr:@supabase/functions-js/edge-runtime.d.ts'
import postgres from 'https://deno.land/x/postgresjs@v3.4.7/mod.js'

const DB=Deno.env.get('SUPABASE_DB_URL')||''
const SUPABASE_URL=Deno.env.get('SUPABASE_URL')||''
const LOCAL_MODEL='deterministic-property-type-v1'
const sql=postgres(DB,{prepare:false})
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff','referrer-policy':'no-referrer'}})
async function sha256(value:string){const d=new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(value)));return[...d].map(b=>b.toString(16).padStart(2,'0')).join('')}
function constantTimeEqual(a:string,b:string){if(a.length!==b.length)return false;let diff=0;for(let i=0;i<a.length;i++)diff|=a.charCodeAt(i)^b.charCodeAt(i);return diff===0}
async function authorized(req:Request){const supplied=req.headers.get('x-listia-ai-dispatch-key')||'';if(!supplied||supplied.length>256||!DB)return false;const[row]=await sql`select decrypted_secret from vault.decrypted_secrets where name='listia_property_ai_dispatch_secret' limit 1`;const expected=String(row?.decrypted_secret||'');return Boolean(expected)&&constantTimeEqual(await sha256(supplied),await sha256(expected))}
async function engineKey(){if(!DB)return'';const[row]=await sql`select decrypted_secret from vault.decrypted_secrets where name='listia_ai_engine_secret' limit 1`;return String(row?.decrypted_secret||'')}
const clean=(value:unknown,max=12000)=>typeof value==='string'?value.trim().slice(0,max):''
const fold=(value:string)=>value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/\s+/g,' ').trim()
const typeAliases:Record<string,string>={house:'house',casa:'house',villa:'house',townhouse:'house',apartment:'apartment',apartamento:'apartment',departamento:'apartment',condo:'apartment',condominium:'apartment',penthouse:'penthouse',land:'land',terreno:'land',lote:'land',lot:'land',commercial:'commercial',comercial:'commercial',office:'office',oficina:'office',warehouse:'warehouse',bodega:'warehouse',nave:'warehouse',building:'building',edificio:'building',hotel:'hotel',local:'local','local comercial':'local'}
function normalizePropertyType(value:unknown){return typeAliases[fold(clean(value,80))]||null}
function evidenceIsGrounded(source:string,evidence:unknown){const e=fold(clean(evidence,240));return e.length>=3&&fold(source).includes(e)}
function sourceText(fields:any){const rows:string[]=[];const add=(label:string,value:unknown)=>{const v=clean(value,label==='description'?12000:1000);if(v)rows.push(`${label}: ${v}`)};add('title',fields?.title);add('operation_type',fields?.operation_type);add('property_type',fields?.property_type);add('description',fields?.description);if(fields?.price!==null&&fields?.price!==undefined&&fields?.price!=='')rows.push(`price: ${String(fields.price).slice(0,80)}`);add('currency',fields?.currency);add('commission_text',fields?.commission_text);add('location_text',fields?.location_text);add('postal_code',fields?.postal_code);return rows.join('\n')}
const localAliases:Array<[string,string]>=[['local comercial','local'],['penthouse','penthouse'],['townhouse','house'],['condominium','apartment'],['departamento','apartment'],['apartamento','apartment'],['apartment','apartment'],['condo','apartment'],['warehouse','warehouse'],['bodega','warehouse'],['oficina','office'],['office','office'],['edificio','building'],['building','building'],['terreno','land'],['land','land'],['lote','land'],['villa','house'],['casa','house'],['house','house'],['comercial','commercial'],['commercial','commercial'],['hotel','hotel'],['local','local']]
function escapeRegex(value:string){return value.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}
function localExtract(fields:any){const explicit=normalizePropertyType(fields?.property_type);if(explicit)return{parsed:{property_type:explicit,property_type_evidence:clean(fields.property_type,80),confidence:1,notes:'explicit_source_field'},provider:'source',model:'explicit-source-field',transport:'source'};const title=clean(fields?.title,300);for(const[phrase,type]of localAliases){const match=title.match(new RegExp(`\\b${escapeRegex(phrase)}\\b`,'i'));if(match)return{parsed:{property_type:type,property_type_evidence:match[0],confidence:.92,notes:'deterministic_title_match'},provider:'listia_local',model:LOCAL_MODEL,transport:'listia_local'}}const description=clean(fields?.description,12000);for(const[phrase,type]of localAliases){const escaped=escapeRegex(phrase);const patterns=[new RegExp(`(?:^|[\\n.;])\\s*(?:tipo\\s+de\\s+propiedad|property\\s+type|tipo\\s+de\\s+inmueble|inmueble)\\s*[:\\-]\\s*(${escaped})\\b`,'i'),new RegExp(`^\\s*(${escaped})\\b`,'i'),new RegExp(`\\b(?:se\\s+vende|en\\s+venta|se\\s+renta|en\\s+renta|for\\s+sale|for\\s+rent)\\s+(?:una?\\s+|un\\s+|an?\\s+)?(${escaped})\\b`,'i')];for(const pattern of patterns){const match=description.match(pattern);if(match)return{parsed:{property_type:type,property_type_evidence:match[1]||phrase,confidence:.84,notes:'deterministic_declared_type_match'},provider:'listia_local',model:LOCAL_MODEL,transport:'listia_local'}}}return{parsed:{property_type:null,property_type_evidence:null,confidence:0,notes:'insufficient_grounded_evidence'},provider:'listia_local',model:LOCAL_MODEL,transport:'listia_local'}}
async function engineExtract(source:string,organizationId:string,propertyId:string){const key=await engineKey();if(!key)return null;const system='You extract factual real-estate fields for LISTIA. Use ONLY the supplied source text. Never infer unsupported facts. For property_type, allowed values are house, apartment, penthouse, land, commercial, office, warehouse, building, hotel, local, or null. Any non-null property_type MUST include a short exact evidence quote copied verbatim from source. If evidence is insufficient return null. Return JSON only.';const prompt=`SOURCE TEXT:\n${source}\n\nReturn exactly these keys: property_type, property_type_evidence, confidence, notes. confidence must be 0 to 1.`;const c=new AbortController(),timer=setTimeout(()=>c.abort(),12000);try{const r=await fetch(`${SUPABASE_URL}/functions/v1/listia-ai-engine`,{method:'POST',headers:{'x-listia-ai-engine-key':key,'content-type':'application/json'},body:JSON.stringify({task_type:'property_extract',quality_tier:'q1',organization_id:organizationId,property_id:propertyId,system,prompt,response_format:'json',max_output_tokens:500}),signal:c.signal});if(!r.ok)return null;const d=await r.json().catch(()=>null) as any;if(!d?.json)return null;return{parsed:d.json,provider:String(d.provider||'listia_ai_engine'),model:String(d.model||d.model_key||'unknown'),transport:'listia_ai_engine',engine_model_key:d.model_key||null,latency_ms:d.latency_ms||null}}catch{return null}finally{clearTimeout(timer)}}
async function extractPropertyType(fields:any,source:string,organizationId:string,propertyId:string){if(normalizePropertyType(fields?.property_type))return localExtract(fields);return await engineExtract(source,organizationId,propertyId)||localExtract(fields)}
async function processOne(){await sql`with ranked as (select id,row_number() over(partition by property_id,job_type order by queued_at desc,id desc) rn from private.property_ai_jobs where status='queued' and job_type='property_extract') update private.property_ai_jobs j set status='cancelled',error_message='superseded_by_newer_input',completed_at=coalesce(j.completed_at,now()),updated_at=now() from ranked r where j.id=r.id and r.rn>1`;const claimed=await sql.begin(async tx=>{const[job]=await tx`select id,property_id,organization_id,job_type,input_fingerprint,input_manifest,attempt_count from private.property_ai_jobs where status='queued' and job_type='property_extract' order by queued_at asc for update skip locked limit 1`;if(!job)return null;const[updated]=await tx`update private.property_ai_jobs set status='processing',started_at=now(),completed_at=null,attempt_count=attempt_count+1,error_message=null,updated_at=now() where id=${job.id}::uuid and status='queued' returning id,property_id,organization_id,job_type,input_fingerprint,input_manifest,attempt_count`;return updated||null});if(!claimed)return{status:'empty'};try{const[property]=await sql`select id,organization_id,title,operation_type,property_type,description,price,currency,commission_text,location_text,postal_code,status,locale,processing_state from public.properties where id=${claimed.property_id}::uuid and organization_id=${claimed.organization_id}::uuid limit 1`;if(!property)throw new Error('property_not_found_or_org_mismatch');const[draft]=await sql`select draft_data,missing_fields,status,version from public.property_drafts where property_id=${claimed.property_id}::uuid and organization_id=${claimed.organization_id}::uuid limit 1`;if(draft?.status==='approved'){await sql`update private.property_ai_jobs set status='cancelled',error_message='draft_already_approved',completed_at=now(),updated_at=now() where id=${claimed.id}::uuid`;return{status:'cancelled',reason:'draft_already_approved',job_id:claimed.id}}const submitted=claimed.input_manifest?.submitted_fields||{};const currentFields={title:property.title||submitted.title||null,operation_type:property.operation_type||submitted.operation_type||null,property_type:property.property_type||submitted.property_type||null,description:property.description||submitted.description||null,price:property.price??submitted.price??null,currency:property.currency||submitted.currency||null,commission_text:property.commission_text||submitted.commission_text||null,location_text:property.location_text||submitted.location_text||null,postal_code:property.postal_code||submitted.postal_code||null};const source=sourceText(currentFields);if(!source)throw new Error('property_source_text_empty');const ai:any=await extractPropertyType(currentFields,source,String(claimed.organization_id),String(claimed.property_id));const candidateType=normalizePropertyType(ai.parsed?.property_type),confidenceRaw=Number(ai.parsed?.confidence),confidence=Number.isFinite(confidenceRaw)?Math.max(0,Math.min(1,confidenceRaw)):0,evidence=clean(ai.parsed?.property_type_evidence,240)||null,grounded=Boolean(candidateType&&evidence&&evidenceIsGrounded(source,evidence)&&confidence>=.65),acceptedType=property.property_type?String(property.property_type):(grounded?candidateType:null);const required:Record<string,unknown>={operation_type:property.operation_type,property_type:acceptedType,location_text:property.location_text,description:property.description,price:property.price,currency:property.currency};const missing=Object.entries(required).filter(([,v])=>v===null||v===undefined||(typeof v==='string'&&!v.trim())).map(([k])=>k),complete=missing.length===0,now=new Date().toISOString(),nextStage=complete?'draft_ready':'needs_input',nextStatus=complete?'ready':'needs_info',oldDraft=draft?.draft_data&&typeof draft.draft_data==='object'&&!Array.isArray(draft.draft_data)?draft.draft_data:{},mergedDraft={...oldDraft,...currentFields,property_type:acceptedType,ai_extraction:{provider:ai.provider,transport:ai.transport,model:ai.model,engine_model_key:ai.engine_model_key||null,processed_at:now,property_type_candidate:candidateType,property_type_evidence:evidence,property_type_grounded:grounded,confidence,source_mode:'submitted_text_only',visual_assets_processed:false}};await sql.begin(async tx=>{if(!property.property_type&&acceptedType)await tx`update public.properties set property_type=${acceptedType},updated_at=now() where id=${claimed.property_id}::uuid and organization_id=${claimed.organization_id}::uuid and property_type is null`;await tx`insert into public.property_drafts(property_id,organization_id,draft_data,missing_fields,status,version,updated_at) values(${claimed.property_id}::uuid,${claimed.organization_id}::uuid,${tx.json(mergedDraft)},${missing}::text[],'draft',1,now()) on conflict(property_id) do update set draft_data=excluded.draft_data,missing_fields=excluded.missing_fields,status=case when public.property_drafts.status='approved' then public.property_drafts.status else 'draft' end,version=case when public.property_drafts.status='approved' then public.property_drafts.version else public.property_drafts.version+1 end,updated_at=now() where public.property_drafts.status<>'approved'`;await tx`update public.property_processing_state set stage=${nextStage},detected_fields=coalesce(detected_fields,'{}'::jsonb)||${tx.json({property_type:acceptedType,ai_provider:ai.provider,ai_transport:ai.transport,ai_model:ai.model,ai_grounded:grounded})},missing_fields=${missing}::text[],processing_completed_at=now(),error_message=null,updated_at=now() where property_id=${claimed.property_id}::uuid and organization_id=${claimed.organization_id}::uuid`;await tx`update public.properties set status=${nextStatus},processing_state=coalesce(processing_state,'{}'::jsonb)||${tx.json({stage:nextStage,next_action:complete?'review_draft':'request_missing_fields',ai_status:'completed',ai_provider:ai.provider,ai_transport:ai.transport,ai_model:ai.model,ai_grounded:grounded,ai_completed_at:now})},updated_at=now() where id=${claimed.property_id}::uuid and organization_id=${claimed.organization_id}::uuid`;await tx`update private.property_ai_jobs set status='completed',provider=${ai.provider},model=${ai.model},result=${tx.json({property_type:acceptedType,candidate_property_type:candidateType,evidence,grounded,confidence,missing_fields:missing,transport:ai.transport,engine_model_key:ai.engine_model_key||null,source_mode:'submitted_text_only',visual_assets_processed:false})},completed_at=now(),error_message=null,updated_at=now() where id=${claimed.id}::uuid`});return{status:'completed',job_id:claimed.id,property_id:claimed.property_id,provider:ai.provider,model:ai.model,grounded,property_type:acceptedType,missing_fields:missing}}catch(error:any){const message=String(error?.message||error).slice(0,500),attempts=Number(claimed.attempt_count||1),retry=attempts<3&&!message.includes('property_not_found');await sql`update private.property_ai_jobs set status=${retry?'queued':'failed'},error_message=${message},started_at=case when ${retry} then null else started_at end,completed_at=case when ${retry} then null else now() end,updated_at=now() where id=${claimed.id}::uuid`;return{status:retry?'retry_queued':'failed',job_id:claimed.id,error:message}}}
Deno.serve(async(req:Request)=>{if(req.method!=='POST')return json({error:'method_not_allowed'},405);if(!(await authorized(req)))return json({error:'unauthorized'},401);if(!DB||!SUPABASE_URL)return json({error:'server_not_configured'},503);const body=await req.json().catch(()=>({})) as any,limit=Math.min(Math.max(Number(body?.limit)||1,1),3),results:any[]=[];for(let i=0;i<limit;i++){const result=await processOne();results.push(result);if(result.status==='empty')break}return json({ok:true,engine:'LISTIA_AI_ENGINE_V1',processed:results.filter(x=>x.status!=='empty').length,results})})
